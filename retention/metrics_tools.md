# 리텐션 메트릭 & 도구 (Retention Metrics & Tools)

> 작성일: 2026-02-14 | 기반: AILEX MVP 기획서, 가격 전략
> 목표: 리텐션 핵심 지표 추적 및 데이터 기반 의사결정 체계 구축

---

## 1. 리텐션 메트릭 개요

### 1.1 왜 리텐션 메트릭이 중요한가

```
SaaS 성장의 핵심 공식:

MRR 성장 = 신규 MRR + 확장 MRR - 이탈 MRR

이탈 MRR이 크면:
- 신규 고객 획득이 "구멍 뚫린 양동이에 물 붓기"가 됨
- CAC 회수 불가 (LTV < CAC)
- 성장 정체 또는 역성장

AILEX 시뮬레이션:
- 월 이탈율 5.5% → 연간 66% 고객 손실
- 월 이탈율 3.5% 개선 시 → 연간 42% 고객 손실
- 차이: 24%p → 12개월 차 MRR 2,000~3,000만원 차이
```

**핵심 원칙**:
- 측정할 수 없으면 개선할 수 없다
- 리텐션 지표는 비즈니스 건강도의 "조기 경보 시스템"
- 1인 운영 → 핵심 지표만 추적 (복잡도 최소화)

### 1.2 AILEX 리텐션 목표

| 지표 | 현재 (기준선) | 6개월 목표 | 12개월 목표 |
|------|-------------|-----------|------------|
| 월 이탈율 (Logo Churn) | 5.5% | 4.0~4.5% | 3.5~4.0% |
| 월 매출 이탈율 (Revenue Churn) | 5.5% | 4.0~4.5% | 3.0~3.5% |
| 비자발적 이탈 비율 | 40% (전체 이탈 중) | 25% | 15% |
| Net Revenue Retention (NRR) | 100% | 105~110% | 110~120% |
| 12개월 고객 유지율 | 44.5% | 55~60% | 65~70% |

---

## 2. 핵심 리텐션 지표

### 2.1 Gross Churn Rate (총 이탈률)

**정의**: 특정 기간 동안 이탈한 고객 또는 매출 비율

**측정 방법**:

#### Logo Churn Rate (고객 수 기준)

```
월 Logo Churn Rate = (이탈 고객 수 / 월초 고객 수) × 100

예시 (AILEX 3개월 차):
- 월초 고객: 50명
- 이탈 고객: 3명
- Logo Churn Rate = (3 / 50) × 100 = 6%
```

#### Revenue Churn Rate (매출 기준)

```
월 Revenue Churn Rate = (이탈 MRR / 월초 MRR) × 100

예시 (AILEX 3개월 차):
- 월초 MRR: 1,500만원
- 이탈 MRR: 90만원 (Pro 3명 × 30만원)
- Revenue Churn Rate = (90 / 1,500) × 100 = 6%
```

**목표**:
- Logo Churn: 월 3.5~4.0%
- Revenue Churn: 월 3.0~3.5%

**SQL 쿼리** (Supabase):

```sql
-- 월 Logo Churn Rate
WITH month_start AS (
  SELECT COUNT(*) AS start_customers
  FROM customers
  WHERE status = 'active' AND created_at < DATE_TRUNC('month', NOW())
),
churned_customers AS (
  SELECT COUNT(*) AS churned_count
  FROM customers
  WHERE status = 'churned' AND churned_at >= DATE_TRUNC('month', NOW())
)
SELECT
  ROUND((churned_count::numeric / start_customers) * 100, 2) AS logo_churn_rate
FROM month_start, churned_customers;

-- 월 Revenue Churn Rate
WITH month_start_mrr AS (
  SELECT SUM(mrr) AS start_mrr
  FROM customers
  WHERE status = 'active' AND created_at < DATE_TRUNC('month', NOW())
),
churned_mrr AS (
  SELECT SUM(mrr) AS churned_mrr_value
  FROM customers
  WHERE status = 'churned' AND churned_at >= DATE_TRUNC('month', NOW())
)
SELECT
  ROUND((churned_mrr_value::numeric / start_mrr) * 100, 2) AS revenue_churn_rate
FROM month_start_mrr, churned_mrr;
```

**해석**:
- Logo Churn > Revenue Churn → 저가 고객이 더 많이 이탈 (긍정적)
- Logo Churn < Revenue Churn → 고가 고객이 더 많이 이탈 (부정적)

---

### 2.2 Net Revenue Retention (NRR)

**정의**: 기존 고객으로부터의 매출이 확장(업그레이드, 추가 구매)과 이탈을 포함하여 얼마나 유지/성장했는지 측정

**계산 공식**:

```
NRR = ((월초 MRR + 확장 MRR - 이탈 MRR - 다운그레이드 MRR) / 월초 MRR) × 100

예시 (AILEX 6개월 차):
- 월초 MRR: 2,000만원
- 확장 MRR: 300만원 (Pro → Enterprise 업그레이드 3명)
- 이탈 MRR: 90만원 (Pro 3명 이탈)
- 다운그레이드 MRR: 0원
- NRR = ((2,000 + 300 - 90 - 0) / 2,000) × 100 = 110.5%
```

**목표**:
- 초기 (6개월): 100~105% (유지 중심)
- 성장기 (12개월): 110~120% (확장 중심)

**벤치마크** (B2B SaaS):
- 100% 이하: 위험 (이탈이 확장보다 큼)
- 100~110%: 양호
- 110~120%: 우수
- 120% 이상: 최상위 (예: Snowflake 158%, Datadog 130%)

**SQL 쿼리**:

```sql
WITH month_metrics AS (
  SELECT
    SUM(mrr) FILTER (WHERE created_at < DATE_TRUNC('month', NOW())) AS start_mrr,
    SUM(mrr) FILTER (WHERE upgraded_at >= DATE_TRUNC('month', NOW())) AS expansion_mrr,
    SUM(mrr) FILTER (WHERE churned_at >= DATE_TRUNC('month', NOW())) AS churned_mrr,
    SUM(mrr) FILTER (WHERE downgraded_at >= DATE_TRUNC('month', NOW())) AS downgrade_mrr
  FROM customers
)
SELECT
  ROUND(((start_mrr + expansion_mrr - churned_mrr - downgrade_mrr) / start_mrr) * 100, 2) AS nrr
FROM month_metrics;
```

**AILEX 전략**:
- 확장 MRR 확보: Free → Pro 전환, Pro → Enterprise 업그레이드 유도
- 이탈 MRR 최소화: 던닝, 취소 플로우, 위험 신호 감지

---

### 2.3 비자발적 이탈 vs 자발적 이탈

**정의**:
- **비자발적 이탈** (Involuntary Churn): 결제 실패로 인한 이탈 (카드 만료, 한도 초과 등)
- **자발적 이탈** (Voluntary Churn): 고객 주도 취소

**측정 방법**:

```
비자발적 이탈 비율 = (비자발적 이탈 고객 / 전체 이탈 고객) × 100

예시:
- 총 이탈: 10명
- 비자발적 이탈: 4명 (결제 실패)
- 자발적 이탈: 6명 (고객 취소)
- 비자발적 이탈 비율 = (4 / 10) × 100 = 40%
```

**목표**:
- 초기: 40% (업계 평균 30~40%)
- 6개월: 25% (던닝 시스템 효과)
- 12개월: 15% (최적화 완료)

**SQL 쿼리**:

```sql
WITH churn_breakdown AS (
  SELECT
    COUNT(*) FILTER (WHERE churn_type = 'involuntary') AS involuntary_count,
    COUNT(*) FILTER (WHERE churn_type = 'voluntary') AS voluntary_count,
    COUNT(*) AS total_churn
  FROM customers
  WHERE churned_at >= DATE_TRUNC('month', NOW())
)
SELECT
  involuntary_count,
  voluntary_count,
  ROUND((involuntary_count::numeric / total_churn) * 100, 2) AS involuntary_pct
FROM churn_breakdown;
```

**해석**:
- 비자발적 이탈 높음 → 던닝 시스템 강화 필요
- 자발적 이탈 높음 → 제품-시장 핏, 온보딩, 가치 전달 개선 필요

---

### 2.4 던닝 회수율 (Dunning Recovery Rate)

**정의**: 결제 실패 후 던닝 시스템으로 회수한 비율

**계산 공식**:

```
던닝 회수율 = (회수 성공 고객 / 결제 실패 고객) × 100

예시 (AILEX 3개월 차):
- 결제 실패: 20건
- 자동 재시도 성공: 8건
- 던닝 이메일 회수: 7건
- 최종 이탈: 5건
- 던닝 회수율 = ((8 + 7) / 20) × 100 = 75%
```

**목표**:
- 초기 (던닝 구현 직후): 50~60%
- 최적화 후 (6개월): 70~80%

**세부 분해**:

```
자동 재시도 성공률 = (자동 재시도 성공 / 결제 실패) × 100
→ 목표: 40~50%

이메일 회수율 = (이메일 회수 성공 / 자동 재시도 실패) × 100
→ 목표: 50~60%
```

**SQL 쿼리**:

```sql
WITH dunning_stats AS (
  SELECT
    COUNT(*) AS failed_payments,
    COUNT(*) FILTER (WHERE recovery_method = 'auto_retry') AS auto_retry_success,
    COUNT(*) FILTER (WHERE recovery_method = 'email') AS email_recovery,
    COUNT(*) FILTER (WHERE recovery_method IS NULL) AS final_churn
  FROM payment_failures
  WHERE created_at >= DATE_TRUNC('month', NOW())
)
SELECT
  failed_payments,
  auto_retry_success,
  email_recovery,
  ROUND(((auto_retry_success + email_recovery)::numeric / failed_payments) * 100, 2) AS recovery_rate
FROM dunning_stats;
```

---

### 2.5 취소 플로우 전환율

**정의**: 취소 시도 고객 중 유지로 전환된 비율

**계산 공식**:

```
취소 플로우 전환율 = (유지 전환 고객 / 취소 시도 고객) × 100

예시:
- 취소 시도: 15명
- 유지 전환: 4명 (할인 수락 2명, 일시정지 2명)
- 최종 취소: 11명
- 전환율 = (4 / 15) × 100 = 26.7%
```

**목표**:
- 초기: 20~25%
- 최적화 후: 25~30%

**대안별 성과**:

```sql
SELECT
  cancellation_reason,
  COUNT(*) AS attempted,
  COUNT(*) FILTER (WHERE outcome = 'retained') AS retained,
  ROUND((COUNT(*) FILTER (WHERE outcome = 'retained')::numeric / COUNT(*)) * 100, 2) AS retention_rate
FROM cancellation_attempts
WHERE created_at >= DATE_TRUNC('month', NOW())
GROUP BY cancellation_reason
ORDER BY retention_rate DESC;
```

**예시 결과**:

| 취소 이유 | 시도 수 | 유지 수 | 전환율 |
|----------|--------|--------|--------|
| 일시적 불필요 | 3 | 3 | 100% (일시정지 선택) |
| 가격 부담 | 5 | 3 | 60% (할인 수락) |
| 사용 빈도 낮음 | 4 | 2 | 50% (온보딩 제안) |
| 기능 부족 | 2 | 0 | 0% |
| 품질 불만 | 1 | 0 | 0% |

**해석**: "일시적 불필요" → 일시정지 옵션이 매우 효과적. "기능 부족" → 제품 개선 필요.

---

### 2.6 Win-back 성공률

**정의**: 이탈 고객 중 Win-back 이메일로 복귀한 비율

**계산 공식**:

```
Win-back 성공률 = (복귀 고객 / 이탈 고객) × 100

예시 (분기별):
- 총 이탈 고객: 100명 (Q1)
- Win-back 복귀: 12명
- 성공률 = (12 / 100) × 100 = 12%
```

**목표**:
- 초기: 8~10%
- 최적화 후: 10~15%

**이메일별 성과**:

```sql
SELECT
  email_sequence,
  COUNT(DISTINCT customer_id) AS sent,
  COUNT(DISTINCT customer_id) FILTER (WHERE reopened = TRUE) AS reopened,
  ROUND((COUNT(DISTINCT customer_id) FILTER (WHERE reopened = TRUE)::numeric / COUNT(DISTINCT customer_id)) * 100, 2) AS reopen_rate
FROM winback_emails
WHERE sent_at >= NOW() - INTERVAL '3 months'
GROUP BY email_sequence
ORDER BY email_sequence;
```

**예시 결과**:

| 이메일 | 발송 수 | 복귀 수 | 복귀율 |
|-------|--------|--------|--------|
| 1차 (Day 7) | 100 | 4 | 4% |
| 2차 (Day 30) | 96 | 3 | 3.1% |
| 3차 (Day 60) | 93 | 9 | 9.7% |
| 4차 (Day 90) | 84 | 2 | 2.4% |
| **총계** | - | **18** | **18%** |

**해석**: 3차 이메일 (Day 60, 할인 오퍼)이 가장 효과적 → 할인 전략 강화.

---

### 2.7 고객 유지율 (Customer Retention Rate)

**정의**: 특정 기간 동안 유지된 고객 비율

**계산 공식**:

```
12개월 유지율 = (12개월 후 남은 고객 / 12개월 전 고객) × 100

예시:
- 12개월 전 가입 고객: 100명
- 12개월 후 남은 고객: 55명
- 12개월 유지율 = (55 / 100) × 100 = 55%
```

**목표** (AILEX):
- 6개월 유지율: 70~75%
- 12개월 유지율: 55~60%
- 24개월 유지율: 40~45%

**SQL 쿼리**:

```sql
WITH cohort AS (
  SELECT id
  FROM customers
  WHERE created_at BETWEEN NOW() - INTERVAL '13 months' AND NOW() - INTERVAL '12 months'
),
retained AS (
  SELECT COUNT(*) AS retained_count
  FROM customers
  WHERE id IN (SELECT id FROM cohort) AND status = 'active'
)
SELECT
  (SELECT COUNT(*) FROM cohort) AS cohort_size,
  retained_count,
  ROUND((retained_count::numeric / (SELECT COUNT(*) FROM cohort)) * 100, 2) AS retention_rate
FROM retained;
```

---

## 3. 보조 지표

### 3.1 Churn Risk Score 분포

**정의**: 이탈 위험 등급별 고객 분포

**추적 항목**:
- Low Risk (0~30점): 전체의 75~80%
- Medium Risk (31~60점): 전체의 15~20%
- High Risk (61~100점): 전체의 5~10%

**목표**: High Risk 비율 5% 이하 유지

---

### 3.2 활성 사용자 비율

**정의**: 지난 30일간 핵심 기능을 1회 이상 사용한 고객 비율

**계산 공식**:

```
활성 사용자 비율 = (활성 사용자 / 전체 고객) × 100

핵심 기능:
- AI 시스템 판정 실행
- 문서 생성
- 대시보드 조회
```

**목표**: 80% 이상

**SQL 쿼리**:

```sql
WITH active_users AS (
  SELECT DISTINCT user_id
  FROM events
  WHERE
    event_name IN ('ai_assessment', 'document_generated', 'dashboard_viewed') AND
    created_at >= NOW() - INTERVAL '30 days'
)
SELECT
  (SELECT COUNT(*) FROM customers WHERE status = 'active') AS total_customers,
  COUNT(*) AS active_users,
  ROUND((COUNT(*)::numeric / (SELECT COUNT(*) FROM customers WHERE status = 'active')) * 100, 2) AS active_user_pct
FROM active_users;
```

---

### 3.3 평균 LTV (Lifetime Value)

**정의**: 고객 평균 생애 가치

**계산 공식**:

```
LTV = ARPU × (1 / 월간 이탈률) × Gross Margin

예시 (AILEX Pro):
- ARPU: 30만원
- 월간 이탈률: 5.5% (0.055)
- 평균 유지 기간: 1 / 0.055 = 18.2개월
- Gross Margin: 80%
- LTV = 30만원 × 18.2 × 0.8 = 437만원
```

**목표**:
- Pro: 400~450만원
- Enterprise: 2,000~2,500만원

---

## 4. 리텐션 대시보드 구성

### 4.1 핵심 지표 대시보드 (Notion/Google Sheets)

```
AILEX 리텐션 대시보드 (2026년 2월)
┌─────────────────────────────────────────────────────────┐
│ 📊 핵심 지표                                             │
├─────────────────────────────────────────────────────────┤
│ MRR:               2,500만원  (▲ 20% MoM)               │
│ 활성 고객:          85명      (▲ 15명 MoM)              │
│ 월 Logo Churn:     4.2%       (목표: 4.5% 이하)         │
│ 월 Revenue Churn:  3.8%       (목표: 4.0% 이하)         │
│ NRR:               108%        (목표: 105% 이상)        │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 🎯 이탈 분석                                             │
├─────────────────────────────────────────────────────────┤
│ 총 이탈:           4명                                   │
│ - 비자발적 이탈:    2명 (50%)                            │
│ - 자발적 이탈:      2명 (50%)                            │
│                                                         │
│ 자발적 이탈 이유:                                        │
│ - 가격 부담:        1명                                  │
│ - 사용 빈도 낮음:   1명                                  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 💰 던닝 성과                                             │
├─────────────────────────────────────────────────────────┤
│ 결제 실패:         8건                                   │
│ 자동 재시도 성공:   4건 (50%)                            │
│ 이메일 회수 성공:   2건 (33%)                            │
│ 최종 이탈:         2건 (25%)                            │
│ 던닝 회수율:       75%  (목표: 70% 이상 ✅)              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 🔄 취소 플로우 & Win-back                                │
├─────────────────────────────────────────────────────────┤
│ 취소 시도:         5명                                   │
│ 유지 전환:         2명 (40%)                             │
│ 최종 취소:         3명 (60%)                             │
│                                                         │
│ Win-back (분기):                                        │
│ 이탈 고객:         30명 (Q1 누적)                        │
│ 복귀 성공:         4명 (13.3%)                           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ ⚠️  이탈 위험 고객                                       │
├─────────────────────────────────────────────────────────┤
│ Low Risk:          68명 (80%)                           │
│ Medium Risk:       12명 (14%)                           │
│ High Risk:         5명  (6%)                            │
│                                                         │
│ High Risk 대응 현황:                                     │
│ - 1:1 이메일 발송:  5/5 (100%)                          │
│ - 통화 완료:        3/5 (60%)                           │
│ - 유지 전환:        2/5 (40%)                           │
└─────────────────────────────────────────────────────────┘
```

### 4.2 Cohort Analysis (코호트 분석)

**정의**: 가입 시기별 고객 유지율 추적

**템플릿** (Google Sheets):

| 코호트 | 가입 수 | Month 1 | Month 2 | Month 3 | Month 6 | Month 12 |
|--------|--------|---------|---------|---------|---------|----------|
| 2025.08 | 30 | 90% (27) | 83% (25) | 77% (23) | 70% (21) | 60% (18) |
| 2025.09 | 45 | 93% (42) | 87% (39) | 80% (36) | 73% (33) | - |
| 2025.10 | 60 | 95% (57) | 88% (53) | 82% (49) | - | - |
| 2025.11 | 70 | 94% (66) | 89% (62) | - | - | - |
| 2025.12 | 80 | 96% (77) | - | - | - | - |
| 2026.01 | 90 | - | - | - | - | - |

**해석**:
- 2025.08 코호트의 12개월 유지율 60% → 목표 55~60% 달성
- 최근 코호트일수록 유지율 높음 → 제품 개선 효과

---

## 5. 추천 도구

### 5.1 무료/저비용 도구 (1인 운영 최적)

| 도구 | 가격 | 기능 | 추천도 |
|------|------|------|--------|
| **Notion** | 무료 | 대시보드, 고객 DB, 코호트 표 | ⭐⭐⭐⭐⭐ (1순위) |
| **Google Sheets** | 무료 | 코호트 분석, 차트, 피벗 테이블 | ⭐⭐⭐⭐⭐ (1순위) |
| **Stripe Dashboard** | 무료 (Stripe 사용 시) | MRR, 이탈률, 구독 통계 | ⭐⭐⭐⭐⭐ (1순위) |
| **Supabase** | 무료 (500MB까지) | SQL 쿼리, 이벤트 추적, 고객 상태 관리 | ⭐⭐⭐⭐⭐ (1순위) |
| **Resend Analytics** | 무료 (Resend 사용 시) | 이메일 오픈율, 클릭률 | ⭐⭐⭐⭐⭐ |

**초기 단계 조합** (총 비용: 0원):
- Notion: 리텐션 대시보드, 코호트 표, 고객 노트
- Google Sheets: 코호트 분석, 차트
- Stripe: MRR, 구독 통계
- Supabase: SQL 쿼리, 이벤트 추적
- Resend: 이메일 성과

---

### 5.2 프리미엄 도구 (성장 후 검토)

| 도구 | 가격 | 기능 | 추천 시점 |
|------|------|------|----------|
| **Baremetrics** | $108/월~ | SaaS 지표 자동 추적, 코호트 분석, 예측 | MRR 5,000만원+ |
| **ChartMogul** | $100/월~ | 구독 분석, MRR 추적, 세그먼트 | MRR 5,000만원+ |
| **Vitally** | $500/월~ | B2B 고객 성공 플랫폼, Health Score | MRR 1억원+ |
| **Mixpanel** | $89/월~ | 이벤트 추적, 코호트, 퍼널 분석 | MRR 3,000만원+ |
| **Amplitude** | $49/월~ | 사용자 행동 분석 | MRR 3,000만원+ |

**도입 순서**:
1. MRR 3,000만원: Mixpanel/Amplitude (이벤트 추적)
2. MRR 5,000만원: Baremetrics/ChartMogul (SaaS 지표)
3. MRR 1억원: Vitally (고객 성공 플랫폼)

---

### 5.3 자체 구현 vs 도구 도입 기준

```
자체 구현 (초기 ~MRR 3,000만원):
✅ 비용 0원
✅ 완전한 커스터마이징
✅ 데이터 소유권
❌ 시간 투자 필요 (주 2~3시간)
❌ 고급 분석 제한

도구 도입 (MRR 3,000만원+):
✅ 자동화
✅ 고급 분석 (예측, 세그먼트)
✅ 시간 절약 (주 10분)
❌ 비용 (월 $100~$500)
❌ 벤더 종속성
```

**AILEX 권장 전략**:
- 런칭~6개월: Notion + Google Sheets + Stripe (무료)
- 6~12개월: 위 도구 + Mixpanel (이벤트 추적) ($89/월)
- 12개월+: Baremetrics 추가 (SaaS 지표 자동화) ($108/월)

---

## 6. 실행 가이드

### 6.1 Notion 대시보드 템플릿

**데이터베이스 구조**:

1. **Customers 테이블**:
   - 이름, 이메일, 플랜, MRR, 가입일, 상태, Churn Risk Score, 마지막 로그인

2. **Churn Events 테이블**:
   - 고객명, 이탈일, 이탈 이유, 이탈 유형 (자발적/비자발적), MRR 손실

3. **Dunning Recovery 테이블**:
   - 고객명, 결제 실패일, 회수 방법 (자동 재시도/이메일), 회수 성공 여부

4. **Winback 테이블**:
   - 고객명, 이탈일, Win-back 이메일 발송일, 복귀 여부, 복귀일

**대시보드 뷰**:
- 월간 요약 (MRR, 이탈률, NRR)
- 이탈 분석 (이유별, 유형별)
- 던닝 성과
- Win-back 성과
- High Risk 고객 목록

---

### 6.2 Google Sheets 코호트 분석 템플릿

**Sheet 1: Raw Data**
- 고객 ID, 가입일, 이탈일, MRR

**Sheet 2: Cohort Retention**
- 행: 가입 월 (코호트)
- 열: Month 1, 2, 3, 6, 12
- 값: 유지율 (%)

**Sheet 3: Cohort Revenue**
- 행: 가입 월
- 열: Month 1, 2, 3, 6, 12
- 값: MRR

**공식 예시** (Month 1 유지율):

```
=COUNTIFS(RawData!$B:$B, ">="&A2, RawData!$B:$B, "<"&EDATE(A2,1), RawData!$C:$C, ">="&EDATE(A2,1)) / COUNTIFS(RawData!$B:$B, ">="&A2, RawData!$B:$B, "<"&EDATE(A2,1))
```

---

### 6.3 주간/월간 리포트 자동화

**주간 리포트** (Slack 발송):

```typescript
// /app/api/cron/weekly-report/route.ts
export async function GET() {
  const metrics = await calculateWeeklyMetrics();

  await sendSlackMessage({
    channel: '#metrics',
    text: `
📊 주간 리텐션 리포트 (${getWeekRange()})

MRR: ${metrics.mrr}만원 (${metrics.mrrGrowth > 0 ? '▲' : '▼'} ${Math.abs(metrics.mrrGrowth)}%)
신규 고객: ${metrics.newCustomers}명
이탈: ${metrics.churnedCustomers}명 (${metrics.churnRate}%)
던닝 회수율: ${metrics.dunningRecovery}%

High Risk 고객: ${metrics.highRiskCount}명
→ 대응 필요: ${metrics.highRiskNotContacted}명
    `,
  });

  return Response.json({ success: true });
}
```

**Vercel Cron**: 매주 월요일 오전 9시 실행

---

## 7. 실행 체크리스트

### 7.1 MVP 단계 (런칭 전 필수)

- [ ] Notion 대시보드 템플릿 구축
- [ ] Google Sheets 코호트 분석 템플릿 생성
- [ ] Stripe 구독 통계 연동 확인
- [ ] Supabase에 이탈 이벤트 저장 로직 구현
- [ ] 던닝, 취소 플로우, Win-back 성과 추적 설정

### 7.2 런칭 후 1개월 (측정 시작)

- [ ] 첫 월간 리포트 생성 (MRR, 이탈률, NRR)
- [ ] 코호트 분석 1차 데이터 입력
- [ ] 던닝 회수율 측정
- [ ] 취소 플로우 전환율 측정
- [ ] 주간 리포트 자동화 설정

### 7.3 런칭 후 3개월 (최적화)

- [ ] 코호트별 유지율 트렌드 분석
- [ ] 이탈 이유 Top 3 파악 및 대응 계획 수립
- [ ] Mixpanel/Amplitude 도입 검토 (MRR 3,000만원 이상 시)
- [ ] 주간 리포트 피드백 반영 (개선 항목 추가)

---

## 8. 참고 자료

- [B2B SaaS Churn Rate Benchmarks: What's a Healthy Churn Rate in 2025?](https://www.vitally.io/post/saas-churn-benchmarks)
- [B2B SaaS Benchmarks: A Complete Guide 2026](https://churnfree.com/blog/b2b-saas-churn-rate-benchmarks/)
- [Full Guide to B2B SaaS Churn Rate Management in 2026](https://churnbuster.io/articles/b2b-saas-churn-rate)
- [Baremetrics Academy: SaaS Metrics Guide](https://baremetrics.com/academy/saas-metrics)

---

## 9. 요약

**AILEX 리텐션 시스템 핵심**:

1. **측정**: 7가지 핵심 지표 (Logo/Revenue Churn, NRR, 던닝 회수율, 취소 전환율, Win-back 성공률, 유지율)
2. **도구**: Notion + Google Sheets + Stripe (무료, 1인 운영 최적)
3. **자동화**: Vercel Cron으로 주간/월간 리포트 자동 생성
4. **목표**: 월 이탈율 5.5% → 3.5~4.0% (12개월 내)

**다음 단계**:
1. 던닝 시스템 구현 (`dunning_system.md`)
2. 취소 플로우 설계 (`cancellation_flow.md`)
3. Win-back 시퀀스 자동화 (`winback_sequences.md`)
4. 이탈 위험 신호 감지 (`churn_signals.md`)
5. **리텐션 메트릭 추적 (본 문서)**

---

**AILEX retention/ 디렉토리 완성**

모든 문서를 검토하시고, 필요한 부분을 MVP에 우선 적용하세요. 질문이 있으시면 언제든 알려주세요!
