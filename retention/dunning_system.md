# 던닝 시스템 (Dunning System)

> 작성일: 2026-02-14 | 기반: AILEX MVP 기획서, 가격 전략
> 목표: 비자발적 이탈(Involuntary Churn) 50-80% 회수 및 연간 매출 손실 8.6% 방지

---

## 1. 던닝 시스템 개요

### 1.1 비자발적 이탈의 임팩트

```
문제 규모:
- B2B SaaS 전체 이탈의 20-40%가 비자발적 이탈 (결제 실패)
- AILEX 월 이탈율 5.5% 중 약 30~40% = 월 1.65~2.2% 비자발적 이탈
- 12개월 차 MRR 8,500만원 기준 → 월 140~187만원 손실 (연간 1,680~2,244만원)
- 던닝 회수율 60% 달성 시 → **연간 1,008~1,346만원 복구 가능**
```

**비자발적 이탈 주요 원인**:
1. 카드 만료 (40~50%)
2. 결제 한도 초과 (20~30%)
3. 은행 거부/사기 탐지 오류 (15~20%)
4. 잔액 부족 (10~15%)
5. 카드 번호 변경 (5~10%)

### 1.2 던닝 시스템 목표

| 목표 | 기준선 (던닝 없음) | 목표 (던닝 구현) | 개선폭 |
|------|-----------------|----------------|--------|
| 비자발적 이탈 회수율 | 0~10% | 60~70% | +50~60%p |
| 자동 재시도 성공률 | 21% (무작위 재시도) | 40~50% | +19~29%p |
| 고객 접점 유지 | 결제 실패 시 즉시 이탈 | 14일 유예 기간 | - |
| 첫 이메일 전환율 | - | 20~30% | - |

---

## 2. 사전 예방 전략 (결제 실패 방지)

### 2.1 카드 만료 사전 알림

**발송 시점**: 카드 만료 7일 전

**이메일 템플릿 1: 카드 만료 알림**

```
제목: [AILEX] 결제 수단 만료 7일 전 - 서비스 중단 방지 안내

안녕하세요, {{고객명}}님.

AILEX를 이용해주셔서 감사합니다.

현재 등록된 결제 수단(카드번호 끝자리 {{카드_끝4자리}})이 {{만료일}}에 만료 예정입니다.

서비스 중단을 방지하기 위해 새로운 결제 수단을 미리 업데이트해주세요.

👉 결제 수단 업데이트: {{결제수단_업데이트_링크}}

업데이트하지 않으시면:
- {{다음_결제일}}에 자동 결제가 실패할 수 있습니다
- AI 기본법 컴플라이언스 문서 접근이 제한될 수 있습니다
- 보관 중인 영향평가서 등 문서가 일시 중단될 수 있습니다

질문이 있으신가요?
이메일로 회신하시거나 {{지원_이메일}}로 문의해주세요.

AILEX 팀 드림
```

**추가 채널**: 인앱 배너 (로그인 시 상단 고정)

```
⚠️ 결제 수단 만료 7일 전입니다. [지금 업데이트하기]
```

### 2.2 Stripe 카드 자동 업데이트

Stripe의 **Automatic Card Updater** 기능 활용:
- 카드사와 연동하여 만료/변경된 카드를 자동으로 업데이트
- 고객 개입 없이 40~60% 자동 해결
- Stripe Dashboard → Settings → Subscriptions → "Automatic card updates" 활성화

**구현 가이드** (Next.js API Route):

```typescript
// /app/api/webhooks/stripe/route.ts
import Stripe from 'stripe';
const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);

export async function POST(req: Request) {
  const payload = await req.text();
  const sig = req.headers.get('stripe-signature')!;

  let event: Stripe.Event;

  try {
    event = stripe.webhooks.constructEvent(
      payload,
      sig,
      process.env.STRIPE_WEBHOOK_SECRET!
    );
  } catch (err) {
    return new Response(`Webhook Error: ${err.message}`, { status: 400 });
  }

  // 카드 자동 업데이트 성공 이벤트
  if (event.type === 'payment_method.automatically_updated') {
    const paymentMethod = event.data.object as Stripe.PaymentMethod;

    // 로그: 카드 자동 업데이트 성공
    console.log(`Card updated: ${paymentMethod.id}`);

    // 사용자에게 알림 (선택사항)
    // await sendEmail({
    //   to: customer.email,
    //   subject: '결제 수단이 자동 업데이트되었습니다',
    //   body: '카드 정보가 안전하게 업데이트되어 서비스가 계속 유지됩니다.'
    // });
  }

  return new Response('Success', { status: 200 });
}
```

---

## 3. 던닝 이메일 시퀀스 (결제 실패 후)

### 3.1 시퀀스 타임라인

```
결제 실패 (Day 0)
    ↓
자동 재시도 1회 (즉시) → 성공률 ~15%
    ↓ 실패 시
자동 재시도 2회 (3일 후) → 성功률 ~10%
    ↓ 실패 시
던닝 이메일 1차 발송 (Day 3) → 전환율 ~25%
    ↓ 무응답 시
자동 재시도 3회 (7일 후) + 이메일 2차 발송 → 전환율 ~15%
    ↓ 무응답 시
이메일 3차 발송 (Day 14, 최종 경고) → 전환율 ~10%
    ↓ 무응답 시
구독 일시중지 (Day 17) + Win-back 시퀀스 시작
```

**중요 원칙**:
- 첫 이메일은 결제 실패 직후가 아닌 3일 후 발송 (자동 재시도 2회 후)
- 재시도만으로 21% 이상 자동 해결되므로, 불필요한 알림으로 고객 불안 조성 방지
- 톤: 부드럽고 협조적 (협박조, 공격적 표현 금지)

### 3.2 이메일 템플릿

---

#### 던닝 이메일 1차 (Day 3) - 부드러운 알림

```
제목: [AILEX] 결제 처리 문제 발생 - 확인 요청

안녕하세요, {{고객명}}님.

AILEX를 이용해주셔서 감사합니다.

{{결제일}}에 진행된 월 결제({{요금제}} 플랜, {{금액}}원)가 처리되지 않았습니다.

걱정하지 마세요. 간단히 해결하실 수 있습니다.

**흔한 원인**:
- 카드 만료
- 결제 한도 초과
- 일시적인 은행 오류

**해결 방법**:
1. 아래 링크에서 결제 수단을 확인/업데이트해주세요
   👉 {{결제수단_업데이트_링크}}

2. 또는 다른 카드로 수동 결제를 진행하세요
   👉 {{수동결제_링크}}

**지금 확인이 어려우신가요?**
{{재시도_예정일}}에 자동으로 다시 시도됩니다.

궁금한 점이 있으시면 이 이메일에 회신해주세요.
항상 도와드릴 준비가 되어 있습니다.

감사합니다,
AILEX 팀 ({{담당자_이름}})

---
P.S. 혹시 AI 기본법 컴플라이언스 관련 질문이 있으시면 함께 문의해주세요.
```

**개인화 변수**:
- `{{고객명}}`: 회사명 또는 담당자명
- `{{결제일}}`: 실패한 결제 시도 날짜
- `{{요금제}}`: Pro / Enterprise
- `{{금액}}`: 30만원 / 100만원
- `{{재시도_예정일}}`: Day 7
- `{{담당자_이름}}`: 실제 팀원 이름 (자동화이지만 인간미 추가)

**A/B 테스트 버전 (제목)**:
- A: "[AILEX] 결제 처리 문제 발생 - 확인 요청"
- B: "{{고객명}}님, AILEX 결제 업데이트가 필요합니다"

---

#### 던닝 이메일 2차 (Day 7) - 긴급감 추가

```
제목: [중요] AILEX 서비스 이용에 지장이 없도록 - 결제 업데이트 요청

안녕하세요, {{고객명}}님.

{{결제일}}부터 결제가 처리되지 않아 다시 연락드립니다.

**현재 상황**:
- 결제 시도: 3회 실패
- 다음 자동 시도: 없음
- 서비스 중단까지 남은 기간: 7일

AI 기본법 컴플라이언스 문서는 법적 의무사항이므로, 서비스가 중단되면:
- 저장된 영향평가서, 투명성 보고서에 접근할 수 없습니다
- 새로운 AI 시스템 판정을 진행할 수 없습니다
- 규제 대응에 공백이 발생할 수 있습니다

**지금 바로 해결하세요** (2분 소요):
👉 결제 수단 업데이트: {{결제수단_업데이트_링크}}

또는 플랜 변경이 필요하신가요?
- 일시적으로 사용하지 않으신다면: 구독 일시정지 옵션
- 예산 조정이 필요하시다면: Free 플랜으로 다운그레이드

👉 플랜 관리: {{플랜_관리_링크}}

도움이 필요하시면 이 이메일에 회신하시거나 {{지원_이메일}}로 연락주세요.
직접 통화도 가능합니다.

{{고객명}}님의 컴플라이언스가 중단 없이 유지되길 바랍니다.

AILEX 팀 ({{담당자_이름}})
{{담당자_전화번호}}

---
이 이메일은 결제 문제로 자동 발송되었습니다.
```

**톤 변화**: 1차 대비 긴급감 증가, 하지만 여전히 협조적. "법적 의무사항"으로 중요성 강조.

**대안 제시**: 결제 수단 업데이트뿐만 아니라 다운그레이드/일시정지 옵션 명시.

---

#### 던닝 이메일 3차 (Day 14) - 최종 경고

```
제목: [최종 알림] AILEX 계정 일시중지 예정 - 3일 내 조치 필요

{{고객명}}님께,

지난 2주간 3차례 연락드렸으나 결제가 완료되지 않았습니다.

**중요 안내**:
📅 {{일시중지_예정일}}에 AILEX 계정이 일시중지됩니다.

일시중지되면:
- 모든 AI 시스템 판정 및 문서 생성 기능 중지
- 저장된 문서 다운로드 불가 (데이터는 30일간 보관)
- 컴플라이언스 대시보드 접근 차단

**지금 바로 복구하세요**:
👉 {{결제수단_업데이트_링크}}

**또는 계정을 정리하고 싶으신가요?**
일시중지 전에 필요한 문서를 다운로드하세요:
👉 문서 다운로드: {{문서_다운로드_링크}}

계정 삭제를 원하시면 여기를 클릭하세요:
👉 {{계정_삭제_링크}}

**혹시 저희가 놓친 부분이 있나요?**
서비스에 불만족스러운 점이 있으시면 솔직히 말씀해주세요.
개선할 수 있는 기회를 주시면 감사하겠습니다.

이메일 회신 또는 {{지원_이메일}}로 연락 주시면
{{담당자_이름}}이 직접 도와드리겠습니다.

AILEX 팀
```

**톤**: 최종 경고이지만 여전히 존중. "계정 삭제" 옵션도 제시하여 투명성 확보.

**피드백 요청**: 서비스 불만족 가능성을 열어두고 피드백 기회 제공 → Win-back 데이터 수집.

---

#### 던닝 이메일 4차 (Day 17) - 일시중지 확정 + 복구 안내

```
제목: AILEX 계정이 일시중지되었습니다 (복구 방법 안내)

{{고객명}}님께,

결제가 완료되지 않아 AILEX 계정이 일시중지되었습니다.

**현재 상태**:
- 계정 상태: 일시중지
- 데이터 보관: 30일간 유지 ({{데이터_삭제일}}까지)
- 복구 방법: 결제 완료 시 즉시 복구

**언제든 복구 가능합니다**:
👉 {{결제_복구_링크}}

결제를 완료하시면:
- 모든 기능 즉시 복구
- 저장된 문서 및 AI 시스템 데이터 그대로 유지
- 추가 비용 없음 (미납 기간 요금만 결제)

**30일 내에 복구하지 않으시면**:
- {{데이터_삭제일}} 이후 모든 데이터가 영구 삭제됩니다
- 영향평가서, 투명성 보고서 등 문서 복구 불가

마지막으로 필요한 문서를 다운로드하세요:
👉 {{문서_다운로드_링크}} ({{데이터_삭제일}}까지만 접근 가능)

그동안 AILEX를 이용해주셔서 감사했습니다.
언제든 돌아오실 수 있도록 준비하고 있겠습니다.

궁금한 점이 있으시면 {{지원_이메일}}로 연락주세요.

AILEX 팀
```

**톤**: 공식적이지만 복귀의 문을 열어둠. "언제든 복구 가능" 강조.

---

### 3.3 이메일 발송 채널

**발신자 설정**:
- 1차: `no-reply@ailex.kr` (자동화)
- 2차: `support@ailex.kr` (인간미 추가)
- 3차: `{{담당자_이름}}@ailex.kr` (개인화)

**이메일 도구**:
- Resend (무료 100통/일 → Pro 고객 70명 기준 충분)
- 템플릿: React Email 기반 반응형 디자인

**트래킹**:
- 오픈율: Resend 내장 트래킹
- 클릭율: UTM 파라미터로 전환 추적
- 회수율: Stripe 웹훅 이벤트로 측정

---

## 4. 자동 재시도 전략

### 4.1 Stripe Smart Retries

Stripe의 **Smart Retries** 기능 활용:
- ML 기반 최적 재시도 타이밍 자동 선택
- 과거 결제 패턴 분석하여 성공 가능성이 가장 높은 시간대에 재시도
- 평균 21% 자동 회수 (이메일 없이)

**설정 방법**:
1. Stripe Dashboard → Billing → Subscriptions → Revenue Recovery
2. "Smart Retries" 활성화
3. 최대 재시도 횟수: 4회 (기본 설정)

### 4.2 커스텀 재시도 스케줄 (Stripe Billing 기본)

Stripe의 기본 재시도 로직:
- 즉시 1회 재시도
- 3일 후 1회
- 5일 후 1회
- 7일 후 1회

**최적화 팁**:
- 월급일 (매월 25일~말일) 타이밍에 재시도 집중
- 오전 9~11시 재시도 (은행 승인율 높음)
- 주말 피하기 (금요일 저녁~일요일 실패율 높음)

### 4.3 재시도 로직 구현 (Webhook 핸들링)

```typescript
// /app/api/webhooks/stripe/route.ts (계속)
export async function POST(req: Request) {
  // ... (이전 코드)

  // 결제 실패 이벤트
  if (event.type === 'invoice.payment_failed') {
    const invoice = event.data.object as Stripe.Invoice;

    // 재시도 횟수 확인
    const attemptCount = invoice.attempt_count;

    if (attemptCount === 1) {
      // 첫 실패: 로그만 기록 (자동 재시도 대기)
      console.log(`Payment failed (attempt ${attemptCount}): ${invoice.id}`);
    }

    if (attemptCount === 2) {
      // 2회 실패: 던닝 이메일 1차 발송 (Day 3)
      await sendDunningEmail({
        customerId: invoice.customer as string,
        template: 'dunning_1',
        invoiceId: invoice.id,
      });
    }

    if (attemptCount === 3) {
      // 3회 실패: 던닝 이메일 2차 발송 (Day 7)
      await sendDunningEmail({
        customerId: invoice.customer as string,
        template: 'dunning_2',
        invoiceId: invoice.id,
      });
    }

    if (attemptCount === 4) {
      // 4회 실패: 던닝 이메일 3차 발송 (Day 14, 최종 경고)
      await sendDunningEmail({
        customerId: invoice.customer as string,
        template: 'dunning_3',
        invoiceId: invoice.id,
      });
    }
  }

  // 결제 성공 이벤트 (던닝 회수 성공)
  if (event.type === 'invoice.payment_succeeded') {
    const invoice = event.data.object as Stripe.Invoice;

    // 이전에 실패했던 결제가 성공한 경우
    if (invoice.attempt_count > 1) {
      console.log(`Dunning recovery success: ${invoice.id} (attempt ${invoice.attempt_count})`);

      // 회수 성공 감사 이메일 (선택사항)
      await sendEmail({
        to: invoice.customer_email,
        subject: 'AILEX 결제가 완료되었습니다',
        body: `안녕하세요, ${invoice.customer_name}님. 결제가 정상적으로 처리되어 서비스가 계속 유지됩니다. 감사합니다.`
      });
    }
  }

  return new Response('Success', { status: 200 });
}
```

### 4.4 대체 결제 수단 제안

**인앱 배너** (결제 실패 고객에게 표시):

```
⚠️ 결제에 문제가 있습니다.
대체 결제 수단 추가: [신용카드 추가] [계좌이체] [PayPal]
```

**Stripe Payment Element 활용**:
- 카드뿐만 아니라 계좌이체, PayPal 등 복수 결제 수단 지원
- 고객이 선호하는 방식으로 결제 가능

---

## 5. Stripe/Paddle 구현 가이드

### 5.1 Stripe 기반 던닝 설정 (추천)

**필수 Webhook 이벤트**:
```typescript
const REQUIRED_EVENTS = [
  'invoice.payment_failed',           // 결제 실패
  'invoice.payment_succeeded',        // 결제 성공 (회수 성공 추적)
  'customer.subscription.updated',    // 구독 상태 변경
  'payment_method.automatically_updated', // 카드 자동 업데이트
];
```

**구독 상태 관리 로직**:

```typescript
// /lib/stripe/subscription-status.ts
export type SubscriptionStatus =
  | 'active'           // 정상
  | 'past_due'         // 결제 실패, 던닝 진행 중
  | 'canceled'         // 취소됨
  | 'unpaid'           // 미납 (최종 실패)
  | 'incomplete';      // 첫 결제 미완료

export async function getSubscriptionStatus(customerId: string) {
  const subscriptions = await stripe.subscriptions.list({
    customer: customerId,
    status: 'all',
    limit: 1,
  });

  if (subscriptions.data.length === 0) {
    return null;
  }

  const sub = subscriptions.data[0];
  return sub.status;
}

// 구독 상태별 접근 제어
export function canAccessFeature(status: SubscriptionStatus | null): boolean {
  if (!status) return false;

  // past_due도 grace period로 접근 허용 (14일간)
  return ['active', 'past_due'].includes(status);
}
```

**Grace Period 설정**:
- Stripe Dashboard → Billing → Subscriptions → "Subscription settings"
- "Grace period for unpaid invoices": **14일** 설정
- 14일 동안 `past_due` 상태 유지, 서비스 접근 가능 (단, 경고 배너 표시)

### 5.2 Paddle 대안 (B2B 특화)

Paddle의 장점:
- VAT/세금 자동 처리 (글로벌 확장 시 유리)
- 구독 관리 UI 내장 (별도 구현 불필요)
- 결제 재시도 자동화

Paddle 던닝 설정:
1. Paddle Dashboard → Retain → Dunning
2. 재시도 스케줄: 3/7/14일 (커스텀 가능)
3. 이메일 템플릿: Paddle 기본 제공 (커스터마이징 제한적)

**한계**: 이메일 개인화가 Stripe 대비 제한적. AILEX는 초기 단계에서 Stripe 추천.

---

## 6. 성과 측정 지표

### 6.1 핵심 KPI

| 지표 | 계산식 | 목표 | 측정 주기 |
|------|-------|------|----------|
| **던닝 회수율** | (회수된 고객 수 / 결제 실패 고객 수) × 100 | 60~70% | 월간 |
| **자동 재시도 성공률** | (재시도 성공 / 전체 재시도) × 100 | 40~50% | 월간 |
| **이메일 전환율** | (이메일 클릭 후 결제 / 이메일 발송) × 100 | 1차: 25%, 2차: 15%, 3차: 10% | 이메일별 |
| **평균 회수 소요일** | 결제 실패 ~ 회수 성공 평균 일수 | 7일 이내 | 월간 |
| **비자발적 이탈률** | (비자발적 이탈 고객 / 전체 고객) × 100 | 1.5% 이하 (전체 5.5% 이탈 중) | 월간 |

### 6.2 대시보드 구성 (Notion 또는 Google Sheets)

```
던닝 성과 대시보드 (월간)
┌─────────────────────────────────────────┐
│ 총 결제 실패 건수: 25건                    │
│ 자동 재시도 성공: 10건 (40%)               │
│ 이메일 회수 성공: 9건 (36%)                │
│ 최종 이탈: 6건 (24%)                       │
│ ───────────────────────────────────────  │
│ 총 회수율: 76% (목표: 60% 초과 달성)       │
│ 회수 매출: 570만원 (Pro 19명 회수)         │
└─────────────────────────────────────────┘

이메일별 성과:
- 1차 이메일 (Day 3): 발송 15건, 전환 5건 (33%)
- 2차 이메일 (Day 7): 발송 10건, 전환 3건 (30%)
- 3차 이메일 (Day 14): 발송 7건, 전환 1건 (14%)
```

### 6.3 A/B 테스트 계획

**테스트 대상**:
1. 이메일 제목줄 (오픈율 영향)
2. CTA 버튼 문구 ("결제 업데이트" vs "결제 수단 확인")
3. 발송 타이밍 (Day 3 vs Day 1)
4. 발신자 이름 (no-reply vs 실명)

**최소 표본**: 각 그룹 30건 이상 (통계적 유의성 확보)

---

## 7. 도구 추천

### 7.1 던닝 전문 도구 (선택사항)

| 도구 | 가격 | 특징 | 추천도 |
|------|------|------|--------|
| **Stripe 내장** | 무료 | Smart Retries, 기본 재시도 로직 | ⭐⭐⭐⭐⭐ (1순위) |
| **ChurnBuster** | $99/월~ | 던닝 이메일 최적화, A/B 테스트 자동화 | ⭐⭐⭐ (성장 후) |
| **ProsperStack** | $199/월~ | 취소 플로우 + 던닝 통합 | ⭐⭐⭐ (성장 후) |
| **Stunning** | $49/월~ | 던닝 이메일 전문 | ⭐⭐ (Stripe 내장으로 충분) |

**초기 단계 권장**: Stripe 내장 기능 + Resend 이메일로 충분. 월 매출 5,000만원 이상 시 ChurnBuster 검토.

### 7.2 이메일 발송 도구

- **Resend** (추천): 무료 100통/일, React Email 지원, 한국어 정상 발송
- **SendGrid**: 무료 100통/일, 전통적 선택
- **Postmark**: 월 $15 (10,000통), 트랜잭션 이메일 특화

---

## 8. 실행 체크리스트

### 8.1 MVP 단계 (런칭 전 필수)

- [ ] Stripe Smart Retries 활성화
- [ ] Webhook 엔드포인트 구현 (`invoice.payment_failed`, `invoice.payment_succeeded`)
- [ ] 던닝 이메일 템플릿 3종 작성 (Resend)
- [ ] 카드 만료 사전 알림 이메일 자동화
- [ ] Grace Period 14일 설정
- [ ] 구독 상태별 접근 제어 로직 구현 (`past_due`도 접근 허용)

### 8.2 런칭 후 1개월 (최적화)

- [ ] 던닝 회수율 측정 (목표: 60% 이상)
- [ ] 이메일 오픈율/클릭율 분석
- [ ] A/B 테스트 1회 실행 (제목줄 또는 CTA)
- [ ] 자동 재시도 성공률 확인 (목표: 40% 이상)

### 8.3 런칭 후 3개월 (고도화)

- [ ] 재시도 타이밍 최적화 (월급일 집중)
- [ ] 고객 피드백 기반 이메일 톤 조정
- [ ] 대체 결제 수단 추가 (계좌이체/PayPal)
- [ ] ChurnBuster 도입 검토 (MRR 5,000만원 이상 시)

---

## 9. 참고 자료

- [Stripe Smart Retries 공식 문서](https://docs.stripe.com/billing/revenue-recovery/smart-retries)
- [SaaS Dunning Emails: 8 Copy-Paste Templates](https://encharge.io/saas-dunning-emails/)
- [Subscription Dunning: Recover 80% of Failed Payments](https://prosperstack.com/blog/subscription-dunning/)
- [How to Recover Failed Payments and Save Lost Revenue](https://baremetrics.com/blog/recover-failed-payments-save-lost-revenue)
- [Full Guide to B2B SaaS Churn Rate Management in 2026](https://churnbuster.io/articles/b2b-saas-churn-rate)

---

**다음 문서**: `cancellation_flow.md` (자발적 이탈 방지)
